(1) Takes 10 integers from the user and stores them in an array

Counts how many numbers are even and odd

Prints:

Total even numbers

Total odd numbers

..........................

(2) Takes N numbers from the user

Stores them in an array

Finds and prints the largest and smallest number

Use loop + if conditions

...........................
(3) Takes 10 integers

Adds only positive numbers using an if statement

Prints the total sum

...........................
(4)
Takes 5 integers into an array

Ask the user to enter a number to search

Use a loop to find it

If found → print "Number found"

Else → "Number not found"

...........................
(5)
Takes 7 integers in an array

Prints which numbers are duplicates

Use nested loops + if condition

...........................
(6)
Takes 5 integers in an array

Prints the array elements in reverse order using a loop

...........................
(7)
akes a string from the user

Counts the number of vowels and consonants

Use loops + if statements

Print both counts

............................
(8)
Take 10 integers from the user.
Rearrange the array so that:

All even numbers appear first

All odd numbers appear last

Do NOT use any built-in sorting functions.

............................
(9)
Take N integers in an array.
Manually find:

The second largest

The second smallest

Without using sorting functions.

............................

(10)
Take 10 integers into an array.
Print how many times each number occurs.

Example:
1, 2, 1, 3, 2, 1
Output:

1 → 3 times  
2 → 2 times  
3 → 1 time
